PROJECT: WorkZilla (monorepo: Django + React + desktop/installer)

PATHS
- Local repo (portable): `~/Desktop/webszilla/saas/work-zilla`
- Office Mac mini user: `guru` -> `/Users/guru/Desktop/webszilla/saas/work-zilla`
- Home MacBook Pro user: `apple` -> `/Users/apple/Desktop/webszilla/saas/work-zilla`
- Server: Hetzner `89.167.16.104`
- Server project path: `/home/workzilla/docker-data/volumes/workzilla_html_data/_data/getworkzilla.com`

HARD RULES (DO NOT BREAK)
- Work only inside repo folder (no outside staging folders).
- Preserve folders:
  - `apps/backend`, `apps/frontend`, `apps/desktop_app`, `apps/bootstrap_installer`
- Do NOT reintroduce legacy “Work Zilla Monitor” popup/process behavior.
- Installer binaries:
  - Do NOT commit/publish binaries inside repo static.
  - Publish installers to Backblaze `application-downloads/`.
  - `/downloads/...` routes must resolve from Backblaze.
- Security:
  - Do not store plaintext passwords/keys in this repo file.

DATABASE SAFETY (CRITICAL)
- Runtime DB is PostgreSQL only (local + production). SQLite runtime is disabled.
- Never sync/copy DB files to server:
  - `*.sqlite3`, `*.sqlite3-journal`, `*.sqlite3-wal`, `*.sqlite3-shm`
- Never overwrite production `apps/backend/.env` from local.
- Code deploy must not change DB unless explicitly asked (migration/data task).

INSTALLERS (WHEN CHANGED)
- Before build: `./scripts/prepare_installer_build.sh`
- After build: `./scripts/upload_installers.sh` (uploads to Backblaze + deletes local binaries)

PRODUCTION POSTGRES (CANONICAL, 2026-03-30)
- `DB_ENGINE=postgresql`
- `DB_NAME=webszilla_db`
- `DB_USER=webszilla_admin2026`
- `DB_HOST=127.0.0.1`
- `DB_PORT=5432`

DB AUTH CHECK (BEFORE RUNNING MIGRATIONS ON SERVER)
- `set -a && source apps/backend/.env && set +a`
- `PGPASSWORD="$DB_PASSWORD" psql -h 127.0.0.1 -U "$DB_USER" -d "$DB_NAME" -c "SELECT current_user, current_database();"`

LOCAL DEV
A) Backend only
- `cd ~/Desktop/webszilla/saas/work-zilla`
- `source .venv/bin/activate`
- `cd apps/backend`
- `python manage.py runserver`

B) Single URL (recommended): Django + built React on same URL
- `cd ~/Desktop/webszilla/saas/work-zilla`
- `./build_and_copy_frontend.sh`
- `source .venv/bin/activate`
- `cd apps/backend`
- `python manage.py runserver 127.0.0.1:8000 --noreload`
- Open: `http://127.0.0.1:8000` (SPA routes under `/app/...`)

C) Mobile app (Expo) browser preview
- `cd ~/Desktop/webszilla/saas/work-zilla`
- `./run_mobile_web_preview.sh`
- Opens Expo web preview for quick mobile UI checks in browser
- Use this for layout/dev checks; final QA should still be done in real phone / emulator

D) Mobile app (Expo) device/emulator preview
- `cd ~/Desktop/webszilla/saas/work-zilla/apps/mobile`
- `npm install` (first time only or after dependency changes)
- `npm run dev`
- Then choose:
  - press `w` for browser preview
  - press `i` for iOS simulator
  - press `a` for Android emulator
  - or scan Expo QR in Expo Go

E) Mobile app release / update method (Codex short note)
- Mobile app is Expo + EAS based.
- EAS project:
  - account: `@webszilla`
  - project: `work-zilla-mobile`
  - project id: `c33ca2bf-0314-41ad-854a-de2c5b92c32f`
- Native install flow:
  - user installs Android APK / AAB or iOS build once
  - after that, JS/UI updates can go through Expo Updates without reinstall if app version / runtimeVersion is unchanged
- Important rule:
  - JS/UI/API-only changes -> prefer EAS Update
  - native dependency / Expo package / permission / app config changes -> new binary build required
- Before mobile build troubleshooting:
  - always run `cd apps/mobile && npm install`
  - then run `cd apps/mobile && npm run typecheck`
  - then run `cd apps/mobile && npx expo-doctor`
- Current build scripts use `npx eas ...` from `apps/mobile/package.json`
- Required mobile env for builds:
  - `EXPO_PUBLIC_API_BASE_URL=https://getworkzilla.com`
  - `EXPO_PUBLIC_UPDATE_CHANNEL=production`
  - optional local overrides: `ANDROID_VERSION_CODE`, `IOS_BUILD_NUMBER`
- Android build command:
  - `cd apps/mobile && EXPO_PUBLIC_API_BASE_URL=https://getworkzilla.com EXPO_PUBLIC_UPDATE_CHANNEL=production ANDROID_VERSION_CODE=<next> npx eas-cli build --platform android --profile preview`
- iOS build command:
  - `cd apps/mobile && EXPO_PUBLIC_API_BASE_URL=https://getworkzilla.com EXPO_PUBLIC_UPDATE_CHANNEL=production IOS_BUILD_NUMBER=<next> npx eas-cli build --platform ios --profile preview`
- iOS note:
  - Apple Developer login/signing may still be required for internal distribution build creation
- Expo login check:
  - `cd apps/mobile && npx eas-cli whoami`
- Build history check:
  - `cd apps/mobile && npx eas-cli build:list --limit 5`

CODEX RUN NOTE
- If user says: `run the mobile app in browser preview`
  - first read this file
  - then run: `cd ~/Desktop/webszilla/saas/work-zilla && ./run_mobile_web_preview.sh`

LOCAL MIGRATIONS (WHEN SCHEMA CHANGES)
- `cd apps/backend`
- `python manage.py makemigrations`
- `python manage.py migrate`

DB TASK STANDARD (WHEN DB CHANGE IS REQUESTED)
- Local: code + `makemigrations` + `migrate`
- Push GitHub immediately (same task window)
- Server: run DB auth check, then:
  - `source venv/bin/activate`
  - `venv/bin/python apps/backend/manage.py migrate --skip-checks`
  - `venv/bin/python apps/backend/manage.py collectstatic --noinput --skip-checks`
  - restart gunicorn

LIVE CRM 403 (CLOUDFLARE) NOTE (2026-04-14)
- Cloudflare blocks `PATCH/DELETE` / method-override for some CRM routes on live.
- Live-safe pattern: use `POST` to collection endpoints + include row id in JSON + `__crm_action: patch|delete`.
- Affected collections:
  - `/api/business-autopilot/leads`
  - `/api/business-autopilot/contacts`
  - `/api/business-autopilot/deals`
  - `/api/business-autopilot/meetings`
  - `/api/business-autopilot/sales-orders`

DEPLOY (GITHUB -> SERVER)
1) Local (any machine) push to GitHub
- `git status`
- `git add .`
- `git commit -m "<message>"`
- `git push origin main`

2) Server pull (code only)
- `ssh root@89.167.16.104`
- `cd /home/workzilla/docker-data/volumes/workzilla_html_data/_data/getworkzilla.com`
- `git config --global --add safe.directory /home/workzilla/docker-data/volumes/workzilla_html_data/_data/getworkzilla.com`
- `git pull origin main`

3) After every code deploy
- Static refresh:
  - `source venv/bin/activate`
  - `venv/bin/python apps/backend/manage.py collectstatic --noinput --skip-checks`
- Restart gunicorn (reliable):
  1) `pkill -f "core_platform.wsgi:application" || true`
  2) `nohup venv/bin/gunicorn apps.backend.core_platform.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 900 --graceful-timeout 60 >/tmp/workzilla_gunicorn.log 2>&1 </dev/null &`
  3) `pgrep -af "core_platform.wsgi:application"`

SERVER GITHUB LINK (ONE-TIME SETUP DONE - 2026-05-08)
- Server folder now has `.git` linked to: `git@github.com:webszilla/work-zilla.git`
- Branch: `main` tracks `origin/main`
- Server pulls from GitHub using repo Deploy Key `workzilla-server`:
  - Private: `/root/.ssh/workzilla_github`
  - Public: `/root/.ssh/workzilla_github.pub`
  - SSH config: `/root/.ssh/config` pins `IdentityFile /root/.ssh/workzilla_github`
- If checkout fails due to untracked files, use safe clean (preserve secrets/venv):
  - `git clean -fdx -e venv/ -e .venv/ -e env/ -e apps/backend/.env -e logs/ -e staticfiles/`

FALLBACK DEPLOY (ONLY IF SERVER GIT PULL FAILS)
- Use rsync and ALWAYS exclude DB files + `.env`:
  - `rsync -az --exclude='.git/' --exclude='env/' --exclude='.venv/' --exclude='venv/' --exclude='apps/backend/.env' --exclude='**/node_modules/' --exclude='**/__pycache__/' --exclude='*.sqlite3' --exclude='*.sqlite3-journal' --exclude='*.sqlite3-wal' --exclude='*.sqlite3-shm' <local_project>/ root@89.167.16.104:/home/workzilla/docker-data/volumes/workzilla_html_data/_data/getworkzilla.com/`

VERIFY (AFTER DEPLOY)
- `curl -I https://getworkzilla.com/` (expect 200)
- `ss -ltnp | grep :8000`
- `pgrep -af "core_platform.wsgi:application"`

ONLINE PUBLISH RECORD (UPDATE EACH DEPLOY)
- Published at (UTC): `2026-06-25T05:22:24Z`
- Local SHA: `0e492a75952df7ce5a8f4c2af9295b234174c142`
- GitHub main SHA: `0e492a75952df7ce5a8f4c2af9295b234174c142`
- Server SHA: `0e492a75952df7ce5a8f4c2af9295b234174c142`
- Deploy method: `git-pull`
- Gunicorn: listening on `:8000` (`yes`)
- Verified URLs:
  - `/` -> `200`
  - `/pricing/` -> `200`
  - `/downloads/bootstrap-products.json` -> `200`
  - `/downloads/windows-agent/` -> `404`
  - `/downloads/mac-agent/` -> `404`

LATEST WORK LOG (2026-06-25)
- Business Autopilot Site Admin quick estimate mobile-only parsing fixed:
  - mobile-only follow-up messages no longer get stored as item description or amount
  - when a fresh mobile is sent mid-flow, stale client/item/amount fields are cleared before continuing
- Business Autopilot Site Admin payment proof upload save fixed for CDN/live:
  - quick estimate edit save now sends new payment proof uploads as multipart FormData instead of large base64 JSON
  - backend converts uploaded proof file into the existing data-url storage format
  - this avoids live 403 failures caused by CDN/WAF on large JSON request bodies
- Business Autopilot Site Admin cancelled estimate hard-delete flow fixed:
  - POST override delete requests now preserve explicit `action: delete`
  - cancelled estimate delete now removes the row instead of re-running cancel logic
- Business Autopilot Site Admin cancelled estimate delete permission hotfix deployed:
  - delete permission now resolves user profile role against the current organization
  - org admins with multiple organization profiles now still get delete access in cancelled estimate rows
- Business Autopilot Site Admin cancelled estimate action visibility hotfix deployed:
  - frontend cancelled-state check now supports both `cancelled` and `canceled`
  - ORG admin delete icon now appears reliably in cancelled estimate rows
- Business Autopilot Site Admin cancelled estimate delete control deployed:
  - cancelled quick estimate rows now show a separate delete action
  - delete option is exposed only for ORG admin/company admin accounts
  - non-admin users are blocked in backend even if they try direct API access
  - cancel flow remains available for active estimates
  - quick estimate payload now includes delete permission flag for UI rendering
  - permission coverage added in backend tests
PREVIOUS WORK LOG (2026-06-24)
- Business Autopilot Site Admin estimate fixes deployed:
  - edit mobile field now shows old mobile suggestions with client name and also supports custom numbers
  - quick estimate edit Save and Cancel buttons aligned to a consistent fixed size
  - normal Send button layout preserved without overlap in composer mode
  - estimate preview now shows notes above `Thank you.` while PDF output stays unchanged
- Business Autopilot role access fix deployed:
  - newly created users can now access dashboard/module pages based on role access records that include `user_type` suffixes
  - frontend CRM/HR permission resolution now matches role + employee role + user type consistently
PREVIOUS WORK LOG (2026-06-15)
- Business Autopilot Site Admin quick estimate workflow deployed:
  - separate Site Admin full-page route and header tab switching
  - externalized Site Admin AI instruction registry for Business Autopilot
  - Quick Estimate creation/edit/delete/share/print flow in Site Admin
  - 2-column Site Admin layout with chat on left and estimate list on right
  - estimate filters, search, pagination, and action icons added
  - edit flow now updates mobile number, client name, and same customer row sync
  - QE Setting popup added for thermal header text with 200-character limit
  - migration `business_autopilot.0036_quickestimate_quickestimatesequence_and_more` applied on server
PREVIOUS WORK LOG (2026-06-13)
- Attendance face-recognition update pulled and deployed:
  - local repo fast-forwarded from GitHub
  - new migration `business_autopilot.0035_attendanceentry_admin_notes_and_more` applied locally
  - server git pull completed
  - server DB auth check passed
  - server migration check completed with no pending migrations
  - collectstatic completed
  - gunicorn restarted
  - live site verified with `200`
- Business Autopilot assistant refinements deployed:
  - wake-word flow tightened for manual Maya-prefixed recording
  - fullscreen assistant layout cleanup and dashboard return behavior
  - icon tooltip cleanup for assistant controls
  - improved Tamil / Tanglish transcription guidance and natural TTS-first playback
  - ticketing-aware direct answers added with total/open ticket summaries
  - large dataset answers now redirect users to manual in-app checking instead of dumping long lists
- Business Autopilot AI assistant upgraded as reusable module:
  - shared assistant registry / prompt context
  - org-aware assistant profile and lightweight training context
  - reusable full-page assistant route for future products
- Admin profile AI Office Assistant settings added:
  - chatbot name
  - male / female voice preference
  - wake word configuration support
- Voice / chat improvements:
  - push-to-talk helpers
  - wake-word detector hooks
  - browser speech input
  - speech playback helpers
  - shared chat history model and sync support
- Business data answer coverage expanded:
  - invoice counts
  - invoice total amount
  - paid / pending / this-month invoice summaries
  - richer org capability / plan / module context for answers
- Full-page assistant UI refined:
  - close returns to dashboard
  - fullscreen route cleanup
  - left sidebar hidden on assistant page
  - extra wrapper box removed
  - full-width layout enabled
- Production deploy completed:
  - GitHub push
  - server git pull
  - DB auth check passed
  - migrations `core.0154`, `core.0155`, `core.0156` applied
  - collectstatic completed
  - gunicorn restarted
- Notes:
  - collectstatic reported duplicate static destination warnings for `public/js/site.js`, `public/css/site.css`, and `js/ai_chatbot_widget.js`
  - warnings did not block deploy; live app verified with `200`

PREVIOUS WORK LOG (2026-06-08)
- Mobile login copy cleaned:
  - removed text: `After login the org admin theme applies automatically.`
- Mobile Business Autopilot dashboard attendance:
  - `My Attendance` title opens attendance records page
  - `In` and `Out` buttons now call geo attendance APIs
  - added mobile attendance records screen
- Backend:
  - added `GET /api/hr/attendance/my-records`
- Deploy scope:
  - code files only
  - do not move DB files
